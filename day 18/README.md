# FastAPI Dockerized Project

This project uses Docker and Docker Compose to manage a FastAPI application, a PostgreSQL database, and a Redis cache.

## Getting Started

### Prerequisites
- Docker Desktop installed and running.

### How to Run
1. Clone this repository.
2. Build and start the services:
   ```bash
   docker-compose up --build

   http://localhost:8000

   docker-compose up